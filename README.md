# snip727-v2 — Uniswap V2/V3 Мониторинг + ruBERT Sentiment

Полностью функциональный бот для мониторинга пулов Uniswap V2/V3 с офлайн анализом настроения ruBERT и стратегией N-of-4. Работает в России без VPN.

## 🚀 Функционал

- **Мониторинг Uniswap V2/V3** — отслеживание создания новых пулов и торговых событий
- **Анализ настроения ruBERT** — офлайн модель `cointegrated/rubert-base-cased-sentence-sentiment` 
- **Стратегия N-of-4** — голосование ≥3 сигналов для алерта
- **Telegram алерты** — мгновенные уведомления о важных событиях
- **Асинхронная архитектура** — высокая производительность
- **Бесплатные RPC** — работает без VPN в России

## 🛠 Стек

- **Python 3.11** — основной язык
- **Web3.py** — взаимодействие с блокчейном Ethereum
- **SQLAlchemy** + **asyncpg** — асинхронная база данных
- **transformers** + **torch** — ruBERT модель для анализа настроения
- **python-telegram-bot** — Telegram интеграция
- **Redis** — кэширование и очереди
- **structlog** — структурированное логирование
- **Poetry** — управление зависимостями

## 📁 Структура проекта

```
snip727-v2/
├── src/snip727/
│   ├── core/           # Конфигурация (Pydantic Settings)
│   ├── db/             # SQLAlchemy модели и сессии
│   ├── bot/            # Telegram бот с командами
│   ├── web3/           # Web3 клиент и мониторинг пулов
│   └── services/       # Sentiment анализ и стратегия
├── tests/              # Тесты с pytest
├── migrations/         # Alembic миграции БД
├── .github/workflows/  # CI/CD pipeline
├── docker-compose.yml  # PostgreSQL + Redis + Bot
└── pyproject.toml      # Конфигурация Poetry
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
poetry install
```

### 2. Конфигурация

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
TELEGRAM_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
DATABASE_URL=postgresql+asyncpg://snip727:snip727@localhost:5432/snip727
REDIS_URL=redis://localhost:6379

# Настройки мониторинга
MIN_LIQUIDITY_USD=10000.0
WHALE_THRESHOLD_USD=50000.0
SENTIMENT_THRESHOLD=0.5
```

### 3. Запуск через Docker Compose

```bash
docker-compose up -d
```

Запустит:
- PostgreSQL 16
- Redis 7  
- Telegram бот с мониторингом

### 4. Запуск локально

```bash
# Миграции БД
poetry run alembic upgrade head

# Запуск бота
poetry run bot
```

## 📱 Команды бота

- `/start` — информация о боте
- `/status` — статус и статистика
- `/pools` — мониторимые пулы
- `/signals` — последние сигналы  
- `/stats` — детальная статистика

## 🔬 Типы сигналов

- **New Pool** — создание нового пула Uniswap V2/V3
- **Liquidity Spike** — резкое добавление ликвидности (≥$10,000)
- **Whale Buy** — крупная покупка (≥$50,000)

## 🎯 Стратегия N-of-4

Система генерирует сигналы на основе событий в пулах. Когда ≥3 однотипных сигнала накапливается за 30 минут, система:
1. Анализирует настроение с ruBERT
2. Рассчитывает общую уверенность
3. Отправляет алерт в Telegram

## 🧪 Разработка

### Линтинг и типы

```bash
poetry run ruff check src/ tests/
poetry run mypy src/
```

### Тесты

```bash
poetry run pytest
poetry run pytest --cov=src/snip727
```

### Миграции БД

```bash
poetry run alembic revision --autogenerate -m "описание"
poetry run alembic upgrade head
```

## 📊 Метрики

- Покрытие кода: 40%+ 
- Все тесты проходят
- CI/CD с GitHub Actions
- Структурированные логи в JSON

## 🔧 Конфигурация RPC

Бот использует бесплатные RPC endpoints, работающие в России:
- `ethereum.publicnode.com`
- `rpc.ankr.com/eth`
- `eth.llamarpc.com`
- `rpc.flashbots.net`

Автоматический failover при недоступности.

## 📝 Лицензия

MIT